const cards = []

function c(knowledgePoint, front, back) {
  cards.push({ subject: 'os', knowledgePoint, front, back, masteryLevel: 0, nextReviewAt: '2026-05-26', reviewCount: 0 })
}

// OS概述
c('操作系统定义', '操作系统的作用和基本功能？', '作用：用户与硬件的接口，计算机资源的管理者。功能：处理机管理、存储器管理、设备管理、文件管理、提供用户接口(命令接口/程序接口/GUI)。')
c('OS四个基本特征', '操作系统的四个基本特征？并发和并行区别？', '1.并发(宏观多程序同时运行，微观串行交替) 2.共享(互斥/同时访问) 3.虚拟(物理→逻辑映射) 4.异步(进程走走停停不可预知)。并发≠并行：并行多核同时执行。')
c('内核态与用户态', '用户态和核心态区别？哪些操作需核心态？如何切换？', '核心态可执行特权指令(关中断/I/O/PSW/基址寄存器/halt)，用户态只能执行普通指令。切换：访管指令(trap/syscall)使用户→核心，PSW模式位标识当前态。')
c('中断与异常', '中断和异常的区别？各举两例？', '中断(外)：CPU外部信号，异步，与当前指令无关(I/O完成，时钟中断)。异常(内)：指令执行产生的条件，同步(缺页/除0/自陷)。中断可屏蔽(IF)，异常不可屏蔽。')
c('系统调用', '系统调用的执行过程？和库函数的区别？', '过程：用户程序→传递参数→trap陷入→内核执行syscall→结果返回。库函数可在用户态完成(fopen内部调open syscall)。系统调用一定涉及内核态切换。')
c('微内核与大内核', '微内核和大内核的各自特点与代表？', '大内核(宏内核)：所有OS服务在内核态(进程管理/内存/文件等)，效率高但耦合重(Linux/Unix)。微内核：最小核心(IPC+调度+低级I/O)，其他服务用户态，灵活但IPC开销大(Minix/QNX)。')
c('进程定义与特征', '进程的定义和三个基本特征？', '进程=程序的一次运行实例(数据段+代码段+PCB)。特征：动态性(生命周期)、并发性、独立性(独立资源分配/地址空间)。程序是静态的，进程是动态的。')
c('进程五状态', '进程五状态模型和各状态转换条件？', '创建→就绪(资源够缺CPU)；就绪→运行(调度选中)；运行→就绪(时间片完/被抢占)；运行→阻塞(等待事件/I/O)；阻塞→就绪(事件发生)。阻塞不能直接→运行。')
c('PCB内容', 'PCB(进程控制块)包含哪些信息？组织方式？', 'PID、处理机状态(寄存器/PC/PSW)、调度信息(优先级/状态)、内存信息(基址/限长/页表指针)、I/O信息(打开文件表)、记账。组织：链表(就绪队列/阻塞队列)、索引表。')
c('原语', '什么是原语？进程的创建/撤销/阻塞/唤醒原语？', '原语是原子操作，执行中不可中断(关中断实现)。Create：建PCB+资源分配+插入就绪。Exit：还资源+PCB清+通知父。Block：PCB进阻塞队列。Wakeup：PCB从阻塞→就绪。')
c('进程通信IPC', '三种主要IPC方式及特点？', '1.共享存储：在共享内存区直接读写，需同步信号量，最快。2.消息传递：send/receive(消息缓冲)，内核中转(fifo队列)。3.管道：半双工FIFO(read/write)，无名管道(父子)和有名管道(任意进程)。另：信号(signal)、Socket。')
c('线程概念', '为何引入线程？线程和进程的区别？', '引入线程目的：减少程序并发开销。进程=资源分配单位，线程=CPU调度单位。线程共享进程资源(地址空间/文件)，各自有栈和PC/寄存器。线程切换开销小。')
c('ULT_vs_KLT', '用户级线程(ULT)和内核级线程(KLT)的区别？', 'ULT：用户空间管理，对内核透明，切换快(不陷内核)，但一阻塞全阻塞，不能多核并行。KLT：内核直接管理线程，可多核并行，切换需陷内核开销大。现代多用KLT或混合模式。')
c('多线程模型', '三种多线程模型的区别？Linux用的是？', '多对一(多ULT→1KLT，不能并行)。一对一(1ULT→1KLT，可并行但内核线程数限)。多对多(多ULT→少KLT，折中最优)。Linux用1:1模型(pthread_creat直接创建内核线程)。')

// 进程调度
c('调度层次', '作业调度的三个层次（长程/中程/短程）？', '长程(作业/高级调度)：从外存后备队列选作业入内存创建进程。中程(中级调度)：挂起/激活进程，平衡内存(调出调进)。短程(CPU/低级调度)：从就绪队列选进程分配CPU，频率最高。')
c('FCFS_SJF_RR_MLFQ对比', 'FCFS/SJF/RR/MLFQ各算法的特点？', 'FCFS：简单，护航效应(长作业造成短作业等)，非抢占。SJF/SPF：平均等待最短，需求预知，长作业可能饿死。RR：公平，q大小影响性能(q大≈FCFS,q小开销大)。MLFQ：多队列优先级+时间片递增，综合最优。')
c('调度器触发时机', 'CPU调度(进程切换)在哪些时机发生？', '1.运行→阻塞(I/O请求) 2.运行→就绪(时间片完/中断) 3.阻塞→就绪(I/O完成，可抢占) 4.进程终止。1和4非抢占不可避；2和3可选择抢占或非抢占。')
c('响应时间vs周转时间', '周转时间和响应时间的区别？', '周转时间=完成时间-到达时间=等待+执行+阻塞时间。带权周转=周转/执行时间。响应时间=首次开始执行时间-到达时间。批处理关注周转，交互系统关注响应。')
c('实时调度', '实时调度算法的RM和EDF？', 'RM(Rate Monotonic)静态优先级：周期越短优先级越高，固定，可调度条件U≤n(2^(1/n)-1)≈0.69。EDF(Earliest Deadline First)动态优先级：截止期越早优先级越高，可调度条件U≤1，比RM灵活但开销大。')
c('优先级反转', '什么是优先级反转？解决方法？', '高优先级进程等低优先级释放共享资源(中等优先级抢占低优先导致无限等待)。解决：优先级继承(低优先级临时升到等待它的最高优先)或优先级置顶协议。')

// 同步与互斥
c('临界区', '临界区的定义？进入/退出临界区的四个准则？', '临界区=访问共享资源(临界资源)的代码段。准则：1.有空让进(无进程在时允许请求者进) 2.忙则等待(已有进程在则其他等待) 3.有限等待(等待时间有限) 4.让权等待(不能进时释放CPU)。')
c('Peterson算法', 'Peterson软件互斥算法的原理？', '两个进程P0/P1，共享数组flag[2]和变量turn。flag[i]=true声明意愿，turn=j让给对方。循环：while(flag[j]&&turn==j)等待。满足互斥+活锁自由+有限等待。仅双进程。')
c('TSL指令', 'Test-and-Set-Lock指令的原理和原子性？', 'TSL是硬件原子指令：读取lock值并设为1(原子操作)。lock=0则获得锁入临界区，lock=1则忙等(spin)。硬件保证原子性(总线锁或Cache一致性)。忙等浪费CPU。')
c('信号量数据结构', '记录型信号量的数据结构和PV操作伪代码？', 'semaphore{int value; process *L;} P:value--;if(value<0)block(); V:value++;if(value<=0)wakeup()。|S.value|=等待队列长度。PV必须成对。')
c('生产者消费者', '生产者消费者问题需几个信号量？核心代码结构？', '3个：mutex=1(互斥),empty=n(空位),full=0(有货)。生产者：P(empty); P(mutex); 放入; V(mutex); V(full)。消费者：P(full); P(mutex); 取出; V(mutex); V(empty)。关键：P(empty/full)在P(mutex)之前，否则死锁。')
c('读者写者问题', '读者优先和写者优先策略的区别？可能导致什么？', '读者优先：新读者到即可读(无写者时)，写者等所有读者离开→写者可能饿死。写者优先：新读者需等当前写者先写→读者可能饿死。公平型：FIFO按到达顺序服务。')
c('哲学家进餐', '哲学家进餐问题的三种解法？', '1.限制最多4人同时拿筷子(信号量count=4) 2.偶数号先左后右，奇数号先右后左(避免对称死锁) 3.同时拿两筷(使用AND型信号量Swait同时等待两个资源)。')
c('管程', '管程(Monitor)的定义和与信号量的比较？', '管程=封装共享数据和操作过程+条件变量，任何时刻最多一个进程使用管程。管程更结构化(面向对象风格)，编译器保证互斥，比信号量更易用但不够灵活。')

// 死锁
c('死锁必要条件', '死锁的四个必要条件？', '1.互斥(资源独用) 2.占有并等待 3.不可剥夺 4.循环等待。互斥不可破(资源固有属性)。预防破坏后三个之一。')
c('银行家算法', '银行家算法核心思想和数据结构？', '安全检查：找是否存在安全序列。Need=Max-Alloc。找Need≤Available的进程执行后释放资源，重复直到所有进程可完成。若存在安全序列则状态安全可分配。数据：Alloc/Max/Need/Available矩阵。')
c('死锁预防', '破坏死锁四个条件的具体方法？', '1.破占有并等：一次性分配全部(利用率低)。2.破不可剥夺：可剥夺资源(保存/恢复现场)。3.破循环等待：资源有序分配(给资源编号，只能递增申请)。最常用第3种(资源排序法)。')
c('死锁检测与解除', '死锁检测方法和解除手段？', '检测：分配图简化法(找不阻塞不孤立的进程逐一释放)，或定期银行家检测。解除：1.撤销进程(按优先级从小到大逐撤) 2.剥夺资源 3.回滚到检查点。')

// 内存管理
c('链接方式', '程序装入的三种方式(绝对/可重定位/动态运行时)？', '绝对装入：编译时知内存地址直接装入。可重定位：装入时基址寄存器+逻辑地址→物理地址。动态运行时：运行时完成地址转换(重定位寄存器+界限寄存器，支持动态移动)。')
c('连续分配', '连续分区分配的四种方式？', '单一连续：单用户任务。固定分区：分区大小固定，有内碎片。动态分区：按需划分大小不等分区，有外碎片。可重定位分区：通过紧凑(compaction)合并碎片。')
c('动态分区算法', '动态分区首次/最佳/最坏/下次适配算法？', '首次适应：找第一个够大的分区分配，最快O(n)。最佳：找最小的够大的，产生小碎片多。最坏：找最大分区，碎片少但大作业难。下次适应(循环首次)：从上分配区之后找，分布较均。')
c('碎片问题', '内部碎片和外部碎片的区别？', '内碎片：分配给进程的分区内未用空间(固定分区中产生，如分区大小>进程需要)。外碎片：未被分配但太小不够任何进程使用的空闲块(动态分区中产生)。紧凑解决外碎片。')
c('分页管理', '分页存储管理的基本概念？页大小选择？', '物理内存分块(页框/物理块)，逻辑地址分页(等大小)。页表记录页号→块号映射。页越小碎片越少但页表越大。通常4KB/Linux透明大页2MB。地址转换：页号查表+页内偏移=物理地址。')
c('页表项结构', '页表项(PTE)包含哪些字段及作用？', '页框号(物理页号)、有效位(页在内存中)、保护位(R/W/X)、修改位(dirty，被写过则换出时需写回)、访问位(reference，LRU用)、缓存禁用位。多级页表中外层页表的一项映射一个内层页表。')
c('TLB(快表)', 'TLB的作用和TALB缺失处理？', 'TLB(Translation Lookaside Buffer)缓存近期使用页表项加速虚实地址转换。TLB缺失：硬件页表漫游(Hardware Page Walk)或软件(OS)处理页表查表→更新TLB。TLB刷新：进程切换时flush(进程地址空间ID ASID可避免全清)。')
c('二级页表', '为什么需要多级页表？二级页表如何节省空间？', '32位4KB页 = 1M个页表项(每项4B需4MB连续内存存储页表)。二级页表：页目录(1024项)+页表(每个1024项)均4KB。只分配实际使用的页表而非全部，节省空间但增加一次访存。')
c('虚拟内存', '虚拟存储器的原理和三个主要特性？', '仅将要用的页调入内存，不用的暂留在外存(disk swap)。特性：多次性(不需一次全调入)、对换性(可换入换出)、虚拟性(逻辑扩容)。基于程序局部性原理。')
c('缺页中断', '缺页中断的处理流程？', '检查页表有效位→无效则缺页→OS：1.找空闲物理页(或用置换算法选换出页) 2.磁盘读取页入内存 3.更新页表项(物理框号+有效位=1) 4.重执行引发缺页的指令。缺页是异常(故障型)可恢复后重执行。')
c('页面置换算法', 'OPT/FIFO/LRU/CLOCK的对比？', 'OPT：换出最远将来使用的页(理论上优但不可实现)。FIFO：先入先出，有Belady异常(多物理框反而多缺页)。LRU：换最久未用，栈算法(无Belady)，接近OPT但开销高。CLOCK：循环链表+访问位，一种近似LRU，实用。')
c('Belady异常', '什么是Belady异常？哪些算法有？', 'FIFO置换在增加物理块数时反而增加缺页次数，称为Belady异常。LRU和OPT是栈算法无此异常。CLOCK也有。原因：FIFO替换页面和页面使用特征不相关。')
c('改进CLOCK', '改进CLOCK如何考虑修改位？', '两轮扫描：第1轮找(A=0,M=0)最佳；第2轮找(A=0,M=1)需写回；第3轮后全恢复访问位(类似一轮)。综合考虑访问位A(最近访问)和修改位M(脏位)，优先淘汰干净页(免写回)。')
c('分配策略', '固定分配/可变分配，局部置换/全局置换的区别？', '固定分配局部：每进程固定物理块数，置换仅在自己页面内选。可变分配全局：OS动态调配，全局选页面置换，灵活但进程之间干扰。可变分配局部：根据缺页率动态调整自身分配。')
c('工作集', '工作集模型的原理？', '进程当前访问的页面集合W(t,Δ)=过去Δ次访问对应的页面集合。缺页率高于阈值则增加物理块，低于阈值则减少。工作集的大小反映了进程的活跃内存需求。')
c('抖动', '什么是抖动(Thrashing)？成因和解决？', '抖动=进程频繁缺页换页忙于换入换出而CPU利用率急剧下降。成因：运行的进程数过多，总工作集超出物理内存可用量。解决：减少多道程序度、增加物理内存、挂起部分进程(中级调度)。')

// 文件系统
c('文件逻辑结构', '有结构文件和无结构文件的区别？', '有结构(记录式)：记录定长/不定长(如数据库行)。无结构(流式)：字节流文件(UNIX/Linux/Windows将一切设备视为流式文件)。现代OS多对流式文件。')
c('文件物理结构', '三种文件物理结构及特点？', '1.连续分配：磁盘上一段连续区，随机访问快但外碎片/增长难。2.链接分配：块指针链接(隐式/显式FAT)，无碎片但随机访问慢。3.索引分配：索引块存所有块指针，随机访问快但索引块额外开销。UNIX用多级索引。')
c('混合索引', 'UNIX混合索引分配方式？各级能存多少数据？', '10个直接块+1个一级间接块+1个二级/三级间接块。假设块4KB每指针4B=1024指针/块。直接10块=40KB，一级=4MB，二级=4GB，三级=4TB。UNIX小文件快(直接)、大文件可扩展(间接)。')
c('FCB', '文件控制块FCB包含哪些信息？', '文件名、文件类型、物理地址(磁盘块号/索引块指针)、文件大小、创建/修改/访问时间、所有者ID/组ID、访问权限(rwx)、链接计数nlink、文件属性。FCB是文件目录项的核心。')
c('目录结构', '单级/两级/树形/无环图的目录结构区别？', '单级：所有文件同一目录(全系统唯一名)。两级：用户文件目录+主文件目录MFD。树形：多级目录层次结构(最常用)。无环图：允许文件共享(增加链接计数)，需防循环。')
c('硬链接vs软链接', '硬链接和符号链接的区别？', '硬链接：不同目录项指向同一inode，links++，删除原文件不影响链接(最后links=0才删)。不可跨文件系统。符号链接(软链接)：独立文件存目标路径，删原文件断链，可跨文件系统。')
c('空闲盘块管理', '空闲表法/空闲链表/位示图/成组链接的特点？', '位示图：每块1bit(0空闲1占用)，实现简单查找快，空间开销小。成组链接法(UNIX)：每组存下组号+空闲块号列表，组间链接。空闲链表：链式，分配回收快但随机访问慢。')
c('文件共享', '文件共享的软硬链接方式和符号链接的优劣？', '硬链接共享：不同路径同一inode，高效但必须同文件系统且不支持目录(防环)。符号链接：灵活可跨文件系统，读链接路径形成新访问路径(慢，需额外解析)。Windows的快捷方式≈符号链接。')
c('磁盘调度算法', '磁盘调度算法FCFS/SSTF/SCAN/CSCAN/Look比较？', 'FCFS：按请求顺序，最公平。SSTF：最近服务优先，总寻道短但远离磁道的饥饿。SCAN(电梯)：来回扫描直到边界。C-SCAN：单向扫描回到头不服务。LOOK/C-LOOK：无请求方向不继续。SSTF寻道时间最优。')
c('文件系统一致性', '文件系统一致性检查什么？', '一致性检查工具(fsck/chkdsk)：1.块一致性(块计数/空闲块与分配块总和) 2.文件一致性(链接计数与目录项匹配) 3.inode与目录一致性。以修复不一致。日志文件系统(ext4/NTFS)通过Journaling减少不一致。')
c('VFS', '虚拟文件系统(VFS)的作用？支持哪些操作？', 'VFS在物理文件系统上提供统一接口(抽象层)，支持不同文件系统共存(ext4/NFS/FAT等)。核心数据结构：superblock、inode、dentry、file。操作：open/read/write/close通过file_operations函数指针。')

// I/O系统
c('I/O控制方式', 'I/O控制的四种方式及各自适用场景？', '1.程序直接控制(轮询)：CPU持续读状态寄存器，利用率极低。2.中断驱动：外设就绪发中断，适合低速设备。3.DMA：批量数据直接内存访问，CPU仅初始和结束参与，适用高速设备(磁盘/网卡)。4.通道：专门I/O处理器，高级。')
c('SPOOLing', 'SPOOLing系统的组成部分和工作原理？', '假脱机：输入/输出井(磁盘)+输入/输出进程。输入井预读数据→虚拟设备供多进程同时用(如多用户打印输出全先输到输出井)。将独占设备变虚拟共享设备，提高利用率和吞吐。')
c('设备独立性', '设备独立性(设备无关性)如何实现？', '逻辑设备名映射物理设备。LUT(逻辑设备表)：逻辑设备名→物理设备(驱动入口)。使用逻辑名编程免于物理设备变化。设备驱动框架(如Linux字符设备/块设备统一接口)。')
c('缓冲区', '单缓冲/双缓冲/循环缓冲的区别？', '单缓冲：1个缓冲区，处理时间C和输入时间T最大(max(C,T)+M,M移数据)。双缓冲：2个交替缓冲，处理=输入时间可重叠(max(T,C))。循环缓冲：多缓冲成环(生产者-消费者模式)，适合突发I/O。')
c('通道', '字节多路/选择/数组多路通道的区别？', '字节多路：分时控制多设备字节交叉传输，适用慢极多设备(打印机/终端)。选择通道：一次只服务一台，全速传输高速设备(磁盘)。数组多路：时分复用多台高速设备阵列交叉传输(成组数据)。')

// 进程同步补充
c('信号量分类', '整型信号量和记录型信号量的区别？', '整型仅value，P通过while(value<=0)忙等。记录型多了阻塞队列，S.value<0时block()进入队列让权等待而非忙等，V时wakeup()唤醒，更优。PC题通常指记录型信号量。')
c('同步前驱关系', '如何用信号量描述进程间的前驱关系？', '每个前驱后继关系建一个信号量初0。前驱执行完成后V(sig)，后继执行前P(sig)。如是DAG图：每条边建一个信号量，确保拓扑序正确执行。')
c('共享内存与内存映射', '共享内存(mmap)与普通共享存储的区别？', 'mmap：将文件映射到进程地址空间直接访问(文件与内存关联)，修改自动写回。System V共享内存(shmget/shmat)：独立于文件的纯内存区域，需显式管理同步。mmap简便可用于IPC和文件I/O。')

export default cards
