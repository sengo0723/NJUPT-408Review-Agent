const cards = []

function c(knowledgePoint, front, back) {
  cards.push({ subject: 'computer-organization', knowledgePoint, front, back, masteryLevel: 0, nextReviewAt: '2026-05-26', reviewCount: 0 })
}

c('存储芯片地址线计算', '给定存储芯片容量，如何计算地址线数和数据线数？', '地址线条数=log₂(存储单元数量)。数据线条数=每个存储单元的数据位数。例：4M×8位芯片→地址=22根(4M=2^22)、数据=8根。芯片容量=2^地址×数据位。')
c('字位同时扩展', '构建存储器需要多片芯片时，字位如何扩展？各芯片连接？', '位扩展(扩字长)：多片并联，地址线/控制线共享，每片各负责不同的数据位→数据线拼接。字扩展(扩容量)：多片数据线共享，片选信号CE+C译码器选择不同芯片→地址扩充。先位扩展后字扩展。总片数=总容量/单片容量。')
c('组合逻辑ALU设计', '16位ALU的进位选择加法器(CSLA)原理？', '将16位分为多组(4位一组)。每组并行计算两路结果(进位输入0和1)，等待上组的进位输入后再用MUX选择正确结果。比串行进位快，比CLA省硬件。速度介于两者之间(近似O(log n))。')
c('浮点特殊值检测', '在浮点运算中如何检测溢出、下溢、非规格化？', '溢出：阶码超过最大可表示值。下溢：结果太小为0(阶码低于所能表示，转为非规格化数或截成0)。非规格化：阶码全0尾数非全0→隐藏位为0。上溢产生±∞(溢出标志置位)，下溢产生非规格化或0(下溢标志)。')
c('双符号位判断', '用双符号位判断溢出时，01和10分别表示？', '01：正溢出（结果的符号位为0但溢出,两正数相加溢出）；10：负溢出（结果的符号位为1但溢出,两负数相加溢出）。双符号位即[sign_ext, sign]用2位表示，正常情况下两位相等(00或11)。')
c('乘法-阵列乘法器', '阵列乘法器(华莱士树)的基本结构？', '被乘数和乘数各位按位与形成部分积→多个加法器阵列(进位保存加法器CSA)逐层压缩部分积，最终两级加法器得出结果。时间复杂度log n(通过Wallace Tree减少到约log_{1.5}n级)，远快于逐位累加O(n)。')
c('补码除法的恢复余数和加减交替', '恢复余数法和加减交替法的区别？', '恢复余数：试商-减除数→够减则商1余数不变，不够则商0再将除数加回去(恢复)。加减交替(不恢复余数)：若不够减商0且下一步加除数，无需恢复。Booth算法类似思想应用于除法。')
c('浮点乘法运算步骤', '浮点乘法的运算步骤？', '1.阶码相加(注意偏移：结果阶=Ex+Ey-偏移(单精度127)。) 2.尾数相乘(定点乘法) 3.规格化(必要时移位调整) 4.舍入。浮点加减更难(需对阶)，而乘除在阶码和尾数分别处理(相对秩序更清晰)。')
c('Cache一致性-MESI', 'MESI协议的四种状态及其转换？', 'Modified：仅本Cache有(已改)，内存陈旧→写回后转Exclusive。Exclusive：仅本Cache有且干净→被其他读转为Shared。Shared：多Cache共享干净→写前使其他Invalid +自身Modified。Invalid：行无效。总线侦听(snooping)实现各Cache状态同步。')
c('写缓存Write Buffer', '写缓存(Write Buffer)的作用？', '写直达写时，CPU将写操作写入Write Buffer后立即继续(不等写完成)，由WB缓冲后异步写入主存。缓解写直时每次访存延迟。写回法替换脏块写回主存也经过WB。小容量FIFO buffer。')
c('Cache缺失类型', 'Cache缺失的三种类型(3C)？', '强制缺失(Compulsory/Cold Miss)：首次访问数据块必须载入(不可避免)。容量缺失(Capacity Miss)：Cache容量不足以装下所有活跃数据块。冲突缺失(Conflict Miss)：直接映射或组相联中因块映射到相同Cache行反复替换。增大相联度可减少冲突缺失。')
c('主存与Cache地址映射计算', '给定32位地址、8KB Cache、16B块、二路组相联，求各字段位宽？', '块:16B→offset=4位。Cache行数=8KB/16B=512行。二路组相联→256组→Index=8位(2^8=256)。Tag=32-8-4=20位。每个Cache行有Tag字段判断是否命中、V有效位、Dirty脏位、LRU位等。')
c('虚拟存储地址转换过程', '详细描述从虚拟地址到物理地址再访问Cache全过程。', 'TLB(快表)查找：虚页号→物理帧号(命中)→形成物理地址。TLB缺失：查找页表(可能在Cache中)→更新TLB→形成物理地址。但若页表项无效→缺页中断换入→更新页表TLB。物理地址→Cache查找(物理索引物理标记)→命中则直接访，缺失则从主存取行填入。')
c('Cache查找策略(虚实)', '物理标记物理索引(PIPT) vs 虚拟标记虚拟索引(VIVT) vs 物理标记虚拟索引(VIPT)？', 'PIPT(标准做法)：对物理地址做索引和标记→需完成地址转换才能查Cache(串行/并行TLB)。VIVT：全用虚拟地址，快但有同义词(别名)问题。VIPT(折中)：用虚拟低位index(在页内不变) + 物理tag，同时访问TLB和Cache(Index翻查同步完成)，最实用。')
c('DRAM刷新类型', 'DRAM的三种刷新方式？', '集中式刷新：在刷新周期末尾将全部行刷一遍(死区时间长)。分散式刷新：每行插入一个刷新周期(各存周期=访问+刷新)(需双倍时间)。异步刷新：均摊刷新时间每t/m间隔刷新一行(m行总数)，将死区均匀分布，最常用。')
c('CD-ROM容量', '光盘的存储原理和容量？CD/DVD/BD各自多少？', '光盘通过凹凸坑记录数据(反射率变化)，螺旋轨道。CD:780nm激光650MB，DVD:650/635nm红激光4.7GB(单面单层)，BD蓝光405nm激光25GB(单层，多层100GB+)。容量=线密度×轨道密度×面积。')
c('SSD与闪存', 'NAND闪存和NOR闪存区别？SSD内部结构？', 'NAND：写块擦，高密度，USB/SD/SSD用。NOR：与NOR门逻辑，字节随机访问，读快写慢，芯片执行XIP。SSD内部：闪存芯片阵列+FTL(闪存转换层实现映射/垃圾回收/磨损均衡)，支持SATA/PCIe(NVMe)接口。')
c('总线的主要性能指标', '总线的主要性能参数有哪些？', '总线宽度(数据线条数，如32/64位)；总线工作频率(时钟频率MHz)；总线带宽=宽度/8×频率/每周期事务数；同步/异步传输方式；总线复用(地址/数据共享线)；总线仲裁方式等。')
c('PCIe总线', 'PCI Express总线的特点和架构？', '基于串行差分传输(不像传统并口PCI)，点到点(每个设备独占通道→lane)，全双工。x1/x4/x8/x16(通道数)，带宽高(PCIe3.0每通道~1GB/s)。采用交换机结构(非共享总线)可并行通信。当前主流CPU-外设互联。')
c('USB基本特性', 'USB总线的特点和版本演变？', '通用串行总线(Universal Serial Bus，广兼容)。拓扑：树形(Tiered Star)最多127设备(Hub可连)。传输类型：控制/批量/中断/等时。USB2.0(480Mbps)，USB3.0(5Gbps-超速，增加SuperSpeed线对)，USB4(40Gbps基于Thunderbolt)。')
c('中断嵌套', '中断嵌套的实现和注意事项？', '高优先中断可打断低优先ISR执行。但进入ISR时CPU应开中断(至少执行到保存现场后开中断)，允许多重中断。低优先级ISR可能要使用原子操作保护临界区。堆栈需确保随时有足够空间容纳嵌套保存。')
c('I/O接口寄存器', 'I/O接口中控制寄存器/状态寄存器/数据缓冲寄存器各自用途？', 'CR(控制寄存器)：CPU发出命令(如开始读/写，中断使能)。SR(状态寄存器)：记录设备状态(忙/闲/出错/数据就绪)。DBR(数据缓冲寄存器)：暂存CPU和设备间交互的数据。这些寄存器即为I/O端口(IO port或MMIO空间)。')
c('DMA控制器的内部结构', 'DMA控制器内含哪些寄存器？传输流程？', 'DMAC内：内存地址寄存器、字数计数器(剩余传输量)、控制/状态寄存器。流程：CPU初始化DMAC→DMAC从设备读取数据并写至指定内存区或反向→递减字数计数器→结束时发出中断。DMA周期挪用发生在CPU不需总线时(未触发总线请求)。')
c('程序中断方式的接口', '中断请求触发器(INTR)和中断屏蔽触发器(MASK)的关系？', 'INTR=1时有中断请求(由设备接口置位)。MASK=1时屏蔽该中断(CPU屏蔽指令设)。中断触发条件=INTR&MASK。此外需要设置中断优先级判优电路(硬件或中断控制器)和中断查询方式。')
c('微指令的格式', '垂直型微指令和水平型微指令各自编码方式？', '水平型：控存字宽很宽(如100+bits)，每一位(或字段)对应一个控制信号(1=使能)，同时可激活多个信号(高度并行)，但编码效率和灵活性低。垂直型：类似机器指令编码(如3位操作码+几种字段)，节省控存但执行多个微指令才能完成一个机器指令(慢)。')
c('微指令的地址形成', '微程序控制器的下一个微地址如何产生？', '方式：1.顺序地址(PC+1增量) 2.跳转(由机器指令操作码映射到对应微程序的入口地址) 3.下址字段(微指令内包含下一微地址字段 指定后续微指令) 4.条件跳转(根据ALU标志/测试结果)。ROM存微程序，比组合逻辑灵活好修改。')
c('流水线-五段经典', 'MIPS五段流水线各段操作详情？每段产生什么流水间寄存器？', 'IF：PC→IM，PC+4→IF/ID寄存器(ID保存PC+4与指令)。ID：读寄存器、立即数扩展、控制信号生成→ID/EX寄存器。EX：ALU运算(有效地址/算术)→EX/MEM。MEM：访存Load/Store(数据存储器)→MEM/WB。WB：写回寄存器。')
c('流水线的暂停机制', '旁路和前推后仍无法解决需要暂停的情况？', 'Load-Use数据冒险：后续指令在EX阶段用到的数据是上一Load指令在MEM阶段才能取出的(无法旁路)。需在ID阶段插入1个bubble(气泡)→nop，又称"Load-use stall"。编译器可重指令调度（scheduling）以减少此类停顿。')
c('超标量的指令发射', '单发射vs多发射，静态调度vs动态调度的区别？', '单发射：每个周期发射1条指令(标量)。多发射(超标量)：每个周期发射2+条指令(ILP。静态调度：编译器重新排序指令(避免相关导致的暂停)。动态调度：硬件(保留站/重排缓冲)打乱指令顺序(Tomasulo算法)，现代CPU用动态调度提高实际ILP。')
c('多核线程模型', 'SMT(同步多线程/Hyper-Threading)与多核的区别？', '多核(片内多处理器)：每个物理核独立(各自完整的Cache/执行单元)。SMT：一个物理核心同时运行多个线程(共享同一套执行资源)，如Intel Hyper-Threading 2个逻辑核共享1个物理核的资源。SMT可在单个核上提升吞吐。')
c('固态盘FTL功能', 'SSD的FTL(闪存转换层)主要功能？', 'FTL将逻辑页地址映射到闪存物理页地址(因闪存不能就地更新→更新需异地写+垃圾回收)。功能：映射表管理、磨损均衡(Wear Leveling)、垃圾回收(GC/整理脏块)、坏块管理、预读策略。是SSD性能与寿命的关键。')
c('中断向量表', '8086实模式下中断向量表的结构？每个向量多大？', '实模式下0-1023共256个向量，每个4字节(CS:IP=段地址:偏移地址)。中断类型号n对应物理地址n×4。0类型(除零)1(单步陷阱)2(NMI)3(断点)...。保护模式下换用中断描述符表IDT(每个8字节)。')
c('外部中断vs异常vs陷入', '三种中断源的区别？复位是什么？', '外部硬件中断(外中断)：INTR/NMI。异常(内中断/内部故障)：执行指令时产生的故障(除0/非法指令/缺页)。陷入(软中断)：INT n或syscall等软件触发(由程序主动产生)。复位：硬件复位信号使CPU回到初始状态，最高优先级。')
c('DRAM访问时序', 'DRAM的读写时序和tRCD/CAS Latency/tRAS？', '行地址选通RAS→tRCD延迟(RAS->CAS Delay)→列地址选通CAS→CL(CAS Latency,列地址到数据输出延迟)→数据输出。tRAS(row active到预充电之间时间)。DRAM较快模式：Page Mode/Burst Mode连续操作当前行内多列(只需发列地址)。')

export default cards
