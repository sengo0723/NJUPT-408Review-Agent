const cards = []

function c(k, f, b) {
  cards.push({ subject:'os', knowledgePoint:k, front:f, back:b, masteryLevel:0, nextReviewAt:'2026-05-26', reviewCount:0 })
}

c('程序的编译链接过程', '编译/链接/装入三个阶段的产物和地址空间的变化？', '编译：源程序→目标模块(.o) (逻辑地址0开始)。链接：多目标模块+库→完整可执行文件(.exe/.out) (逻辑地址空间统一)。装入：可执行文件→内存(将逻辑地址重定位为物理地址)。分段/分页在装入阶段建立映射。')
c('存储器分配算法比较', '首次适应、循环首次、最佳适应、最坏适应的碎片产生？', '首次：外部碎片多但分配快。循环(下次适应)：碎片分布均匀(平均得多)。最佳：外部碎片最多(小块更碎)却空闲区出现大块概率低。最坏：大块分割快。选最坏算法反而产生最少的平均外部碎片(实际不太用)。')
c('快表的刷新', '上下文切换时TLB如何处理？TLB Shootdown？', '进程切换：TLB中旧进程的映射无效(需刷新)——全部无效或FLUSH特定ASID的项。多核TLB一致：一个核修改页表后需通知其他核将对应TLB项作废(IPI→TLB shootdown)。ARM/Linux使用ASID+TLB无效接口。')
c('页的换入换出策略', '全局置换与局部置换、可变分配与固定分配的对比？', '固定分配局部：每进程固定物理块数，置换限于自己的页面(简单但缺页率高)。可变分配全局：OS动态调块数，全局选换出页(会导致进程线程间干扰)。可变分配局部：按缺页率调每个进程的物理块数(兼顾灵活和隔离)。')
c('工作集时钟算法', 'WSClock算法如何结合工作集和CLOCK算法？', '结合工作集(最近Δ次访问的页面集合)和CLOCK(循环链表+访问位)。检查指针经过的页面：若访问位=1则可能在工作集中跳过；若访问位=0且超出窗口时间则回收该页；否则继续下一轮。降低缺页率和实现较简单。')
c('缺页率PFF算法', 'PFF(Page Fault Frequency)算法的原理？', '测量每个进程的缺页频率PFF。PFF高(超过上限)→增加分配的物理块数。PFF低(低于下限)→减少分配块数(换出部分)。通过动态调整块数平衡各进程的缺页率，减少整体抖动。属可变分配策略。')
c('页面的换出到交换空间', '换出的页面存在哪里？交换空间和文件系统在磁盘上的区别？', '匿名页(进程的堆/栈/数据段)换出到交换空间(swap分区或swap文件)；文件映射页(对应的文件执行/读取的页)换出时不需写回(因为磁盘已有)但需要重读。交换区在原始磁盘上通常有更优的布局(连续区)。')
c('LRU的栈实现', 'LRU页面置换算法的栈实现如何做到O(1)？', '用栈保存页面号序列(最近使用的在栈顶)。每次访问页号：若在栈中，移到栈顶；若不在则从栈底(最久未用的)淘汰，新页插入栈顶。但栈在硬件/O(1)中实现不易，软件实现用双向链表+Hash表。近似LRU常用CLOCK。')
c('文件打开表结构', '系统打开文件表、用户打开文件表、FCB的关系？', '每个进程有用户打开文件表(fd→系统打开文件表项指针)。系统维护全局打开文件表(一项=文件偏移量+访问权限+指向FCB的指针)。FCB(inode)存在磁盘(文件的基本元数据)。fork时父子继承同样文件表项(共享偏移量)。')
c('文件的存储空间分配', '混合索引中各级索引能表示的最大文件大小？', '假设块4KB，块指针4B→每索引块有1024个块指针。10个直接块=40KB。一级间接块=1024×4KB=4MB。二级间接块=1024×1024×4KB=4GB。三级间接块=4TB。此即Unix inode的13个块指针的模式。')
c('目录和硬链接', '目录条目和inode的关系？一个inode可以有多个目录条目指向吗？', '目录=文件名→inode号的映射表的文件。多个目录条目指向同一个inode即硬链接(links > 1)。删除文件只删除目录条目；只有当链接数links=0且无进程打开时才释放inode和块。软链接(符号链接)是独立的特殊文件存路径。')
c('文件系统缓冲', '页高速缓存(Page Cache)的作用？', '文件读写的缓冲在物理内存(Page Cache)。read:先查页缓存，命中则直接拷贝到用户缓冲区(避免磁盘IO)，未命中则从磁盘读取放入页缓存。write:write()先写入页缓存(脏页)，稍后异步写回(write back)，用flush/fdatasync可强制。')
c('磁盘空闲空间管理方法对比', '位示图法和成组链接法各自的效率对比？', '位图：每块1bit→n块占据n bit，分配/回收通过置位/清零。成组链接(unix)：将空闲块分组成链表(一组存下一组号+空闲块号表)，每盘仅存一个超级块(起始数据)。比位图更省空间，但碎片恢复复杂。')
c('I/O软件层次', 'I/O软件的四个层次？', '1.中断处理程序(底层)。2.设备驱动程序(各设备厂家代码，提供统一接口)。3.与设备无关的操作系统软件(统一命名/保护/缓冲/错误报告)。4.用户层I/O软件(库函数如printf/fopen)。各层隐藏底层具体实现细节。')
c('SPOOLing系统应用', 'SPOOLing系统的输入井和输出井的作用？', '为慢速独占设备在磁盘开缓冲区：井充当虚拟设备。多用户共享同一个物理设备的打印工作被假脱机到输出井各进程各自的打印文件，由spooling守护进程逐个输出。输入井预输入卡片/终端等数据。')
c('设备驱动程序接口', 'Linux设备驱动程序的file_operations结构体常见函数？', 'open/release/read/write/ioctl/lseek/mmap/fsync等函数指针。驱动只需实现所需操作(如键盘无需lseek)，不必全部。VFS通过file_operations调用特定驱动，屏蔽硬件差异。字符设备和块设备的struct不同。')
c('I/O调度算法在块设备层', 'Linux块层的电梯调度算法(deadline/CFQ/noop)各自特点？', 'noop(无调度):FIFO合并相邻请求(用于SSD随机访问快)。deadline:保证读写请求有最大延时上限(防止饥饿)。CFQ(完全公平队列):为每个进程分配时间片和I/O带宽，公平。磁盘的情况下CFQ和deadline综合考虑。')
c('自旋锁vs信号量vs互斥量', '内核同步机制中自旋锁/信号量/互斥量的使用场景？', '自旋锁(spinlock)：忙等，持锁时间极短(不允许阻塞)且CPU不予调度→SMP核间同步。信号量(semaphore)：可阻塞睡眠，适合持锁时间长或需同步进程数多(如PV操作)。互斥量(mutex)：=二值信号量(加锁互斥)但更高效(有所有者身份检测/优先级继承)。')
c('RCU同步机制', 'RCU(Read-Copy Update)的基本思想？适用场景？', '读者无需锁，写者复制数据→修改→原子指向新数据→旧数据在grace period后释放(所有读者退出临界区)。适用于读远多于写的场景(Linux内核中的路由表/netfilter等)。')

export default cards
