const cards = []

function c(knowledgePoint, front, back) {
  cards.push({ subject: 'data-structure', knowledgePoint, front, back, masteryLevel: 0, nextReviewAt: '2026-05-26', reviewCount: 0 })
}

c('树的存储结构', '树的三种存储结构？', '1.双亲表示法：数组存每个节点的数据+双亲下标。2.孩子表示法：邻接表(每个节点一个孩子链表)。3.孩子兄弟表示法(二叉链表)：firstChild+nextSibling。各有适用场合。')
c('二叉树带权路径长度', '二叉树的最小路径长度MPL和带权路径长度WPL？', 'MPL=Σ(根到所有节点路径长)，完全二叉树MPL最小。WPL=Σ(叶子权值×路径长)，哈夫曼树WPL最小。两个概念不同：MPL考虑所有节点(含内部)，WPL只考虑叶子。')
c('栈与递归调用', '函数调用压栈的过程？栈帧包含什么？', '调用函数时在系统栈分配栈帧(活动记录)：参数、局部变量、返回地址、帧指针(old EBP)。返回时弹栈帧恢复上一层执行。递归调用层数过多导致栈溢出(stackoverflow)。')
c('优先队列实现', '优先队列可以用哪些数据结构实现？复杂度？', '无序数组/链：插入O(1)，删除max O(n)。有序数组/链：插入O(n)，删除max O(1)。二叉堆：插入/删除O(logn)，建堆O(n)，空间O(1)。已是最优实现。')
c('双端队列的栈模拟', '如何用两个栈实现队列？复杂度？', '栈1用于入队(push)，栈2用于出队。出队时：若栈2空则栈1全部倒入栈2。均摊O(1)。入队每次O(1)。同理用两个队列可以实现栈。')
c('BST的查找效率', 'BST查找长度ASL如何计算？等概率下插入序列对形状的影响？', '等概率下ASL=Σ(深度×1/n)。最佳：平衡BST ASL≈O(logn)。最差：有序序列插入→退化成链表ASL≈O(n)。插入序列顺序决定BST形状。平均随机插入约1.39log₂n。')
c('BST插入序列与形状', '给定1到n的有序序列，有多少种BST形状？', '卡特兰数Cₙ。因为中序遍历必是1到n有序序列，BST的形状完全由先序/插入顺序决定。此处等价于前序序列为入栈序列，BST形状=合法出栈序列的卡特兰数。')
c('堆排序实现细节', '堆排序升序为什么用大根堆？空间O(1)如何实现？', '升序用大根堆：堆顶(arr[0])最大，和末尾交换，堆大小-1，下滤arr[0]。重复得升序。整个过程在原数组原地操作(输出区+堆区)，无需辅助数组→空间O(1)。')
c('快排空间复杂度分析', '快排最坏空间复杂度O(n)和平均O(logn)的原因？', '递归调用栈深度取决于划分的均衡程度。每次选最小/最大元素划分→递归深度n→空间O(n)。三数取中/随机化后期望划分均衡→深度O(logn)→空间O(logn)。尾递归优化可降低到O(logn)。')
c('基数排序vs计数排序', '基数排序与计数排序的关系？计数排序是稳定的吗？', '基数排序的每趟排序用稳定排序(可用计数排序)。计数排序：统计各值频次→累积频次→从后向前放入正确位置。计数排序是稳定的(从后向前处理确保后出现的同值元素位置更后)。')
c('桶排序', '桶排序(Bucket Sort)的原理和期望复杂度？', '将元素分配到不同范围的桶中→每个桶内单独排序→合并。假设均匀分布，期望O(n)。桶数≈n时最佳。最坏情况元素集中在一个桶→退化为O(nlogn)或O(n²)。适合均匀分布数据。')
c('插入排序与希尔排序的关系', '为什么希尔排序效率更高？最后一遍d=1时为何快？', '希尔通过增大间隔进行预排序，使大元素快速后移小元素快速前移。d递减到1时序列已经基本有序，直接插入排序对基本有序序列O(n)~远好于O(n²)。本质：让插入排序在增量层面跨距离交换。')
c('堆的下滤与建堆复杂度', '堆的下滤操作的最坏比较次数？证明建堆O(n)。', '下滤：节点需和两个孩子中较大的比较→最多2次比较/层。第h层(从底算)元素数最多⌈n/2^(h+1)⌉个，每个下滤≤h次。总比较次≤Σh·⌈n/2^(h+1)⌉·2≈2n。所以建堆O(n)。')
c('B树的关键字数与高度', 'm阶B树的最小最大关键字数？最大高度和最小高度？', '最小关键字数=1+2(⌈m/2⌉+⌈m/2⌉²+...+⌈m/2⌉^(h-2))≤n。最大高度≤log_{⌈m/2⌉}((n+1)/2)+1。最大关键字数=(m-1)(1+m+m²+...+m^(h-1))=m^h-1。从而最小高度≥log_m(n+1)。')
c('B树的删除分支', 'B树删除时可能合并哪些节点？', '删除关键字后少于⌈m/2⌉-1个：检查左右兄弟，若有多的(>⌈m/2⌉-1)则借一个；若都不多则与兄弟+父节点中间关键字合并为一个节点。合并可能导致父节点关键字减少，可能逐层向上合并。')
c('B+树与B树的存储差别', 'B+树内部节点不存记录指针，有什么优势？', '内部节点不含数据只做索引，可存更多关键字(≈2倍)→树更矮(磁盘I/O更少)。且所有数据存储在叶子层，查找性能稳定。适合数据库(MySQL InnoDB主索引即B+树)实现。')
c('散列表装载因子与扩容', '散列表何时扩容？扩容后的复杂度？', '当装载因子α超过阈值(如0.75)需要扩容。扩容：表长×2→取新的素数→所有元素rehashing重新插入新表。rehashing总时间O(n)但经均摊分析仍为O(1)(惰性策略)。')
c('线性探测的聚集', '线性探测为何容易出现一级聚集Primary Clustering？', '增量1每次检测相邻的下个位置→冲突的元素会簇集(连续多个被占位，下一个空闲位离冲突点越来越远)。聚集增长时，新插入元素再落入这个聚集区的概率正比于聚集大小→形成自增强效应→第i次探测在最坏时与i有关。')
c('平方探测与双重散列', '平方探测可能面临什么问题？双重散列如何解决？', '平方探测可避免一级聚集，但所有同义词探测序列完全一样(二级聚集Secondary Clustering)。双重散列：第2个散列函数作用于关键字使不同同义词的探测步长不同，消除所有聚集，最接近随机探测。')
c('快排的枢纽选择策略', '选择pivot的三种策略？各自优缺点？', '1.首/尾元素：简单但有序时最坏O(n²) 2.随机选取：期望好(证明平均O(nlogn))，随机数生成开销小 3.三数取中(首/尾/中取中位数)：实践中最好(确保不被最坏输入影响)。STL sort使用三数取中+递归小规模转插入排序。')
c('归并排序迭代版', '归并排序非递归(迭代)时如何实现？', '步长从1开始每次×2：同规模子段两两归并。外层循环 while(len < n) { 内层2段归并:merge(i,i+len-1,min(i+2*len-1,n-1)) }。空间O(n)。避免递归开销但原理不变。')
c('堆和优先队列的关系', '二叉堆为什么适合实现优先队列？', '二叉堆满足堆序性(最大/小元素在堆顶)→getMax O(1)、deleteMax O(logn)(下滤)、insert O(logn)(上滤)。且用数组存完全二叉树、空间连续、缓存友好。均为优先队列最佳实现。')
c('链表归并', '两个有序单链表的归并算法？原地归并？', '双指针比较两链表当前节点，较小者接到新链表尾。O(m+n)。若需要原地(in-place)则更难：需调整next指针，不留坑。可用于链表的归并排序。')
c('循环链表判空', '不带头结点的循环链表如何判空？', '无头结点的循环单链表：L==NULL为空(因无节点)。带头结点：L->next==L为空。判尾节点：p->next==L(首节点)而为尾节点(循环特性)。')
c('栈混洗算法', '枚举所有合法出栈序列的算法？', '回溯法：递归尝试入栈(尚有元素时)或出栈(栈非空时)，若某步出栈>入栈则剪枝。产生所有卡特兰数种排列。可用于验证序列合法性(给定序列模拟栈操作)。')
c('BFS与DFS的应用', 'BFS和DFS分别适用于哪些算法问题？', 'BFS：无权最短路径、连通分量、二分图检测、最小生成树(Prim)、层序遍历。DFS：判环、拓扑排序、强连通分量SCC、连通分量(也可)、桥与割点、全排列、回溯搜索。')
c('最小生成树的Prim实现', 'Prim两种具体实现方式(dist数组法和优先队列法)？', 'dist法：未加入节点到MST的最近距离。O(V²)适合邻接矩阵稠密图。优先队列法：每次用堆选最小权候选边，O((V+E)logV)适合邻接表稀疏图。两者都是贪心扩展MST。')
c('Kruskal的并查集实现', 'Kruskal算法使用并查集判环的伪代码？', '1.边按权升序排列 2.初始化并查集(每点各成一集合) 3.遍历每条边(u,v)，若Find(u)≠Find(v)，则加入MST, Union(u,v)。若等则形成环舍弃。重复直到n-1条边。O(ElogE)(排序)+O(E×α(V))(并查集)。')
c('图的邻接多重表', '邻接多重表(无向图)与邻接表的区别？', '邻接多重表每边仅一个节点(而不是邻接表中每条边出现两个节点)，边节点双向链接对应两顶点的边链。存储空间省(无冗余边)且便于删除操作。适用于对边频繁操作的场景。')
c('函数时间复杂度分析技巧', '分析递归算法时间复杂度的常用方法？', '1.Master定理(T(n)=aT(n/b)+f(n)) 2.递归树法(画递归展开层次，每层开销求和) 3.代入法(猜测解+归纳证明) 4.迭代展开法(将递推代入自身直到base case)。M.T.最常用，需正确识别a,b和f(n)。')
c('平衡二叉树的高度证明', '含n个节点的AVL树高度最多是多少？', '设N_h为高度h的AVL最小节点数。N_0=1,N_1=2,N_h=N_{h-1}+N_{h-2}+1。N_h≈φ^(h+1)/√5-1 (φ=1.618黄金比)。反解h≤1.44log_2(n+1)。故AVL查找最坏1.44log_2n。')
c('树与二叉树的遍历对应', '树的先根/后根/层次遍历对应二叉树的？', '先根=转二叉树的先序遍历。后根=转二叉树的中序遍历(左右根→左根右)。层次=转二叉树的层序遍历。这种对应使我们能用二叉树的遍历方法处理一般的树形结构。')
c('图的存储-十字链表', '有向图的十字链表存储结构？', '正交链表：顶点节点(数据+firstIn弧+firstOut弧)。弧节点(弧尾顶点+弧头顶点+同弧头的下条弧+同弧尾的下条弧+权值)。适合频繁计算入度/出度的有向图应用。')
c('非连通图的遍历', '对非连通图做BFS/DFS，如何确保所有节点被访问？', '必须外部循环遍历所有顶点，遇到未被访问的顶点就BFS/DFS一次。调用BFS/DFS的次数=连通分量个数。若为有向图则DFS次数没有意义(需SCC算法)。')
c('有向图的逆邻接表', '逆邻接表(逆邻表)的用途？', '对于入度计算频繁的有向图问题，除邻接表(出边表)之外维护逆邻接表(入边表)。每个顶点的入边链表记录了所有弧头为该顶点的边，便于快速计算入度。')
c('哈希函数的冲突均匀', '好的哈希函数应该满足哪些条件？', '1.确定性(同样输入必定同样输出) 2.均匀分布(关键字映射到各散列地址等概率) 3.计算简单快速 4.雪崩效应(输输入微小变化→输出巨大变化)。用素数取模确保分布均匀。')

export default cards
