const cards = []

function c(k, f, b) {
  cards.push({ subject:'os', knowledgePoint:k, front:f, back:b, masteryLevel:0, nextReviewAt:'2026-05-26', reviewCount:0 })
}

c('操作系统接口分类', '操作系统的三种用户接口？', '1.命令接口(命令行/CLI) 2.程序接口(系统调用API) 3.图形用户接口(GUI)。系统调用是用户程序获取OS服务的唯一途径。Windows GUI和Linux GUI都是调用底层系统调用。')
c('大内核与微内核的性能对比', '为何Linux等主流系统用宏内核而非微内核？', '宏内核效率高(一次函数调用无需IPC)，功能都在内核态；微内核IPC开销大(消息传递)，但模块化隔离好。Linux实际采用混合：内核模块动态加载支持多种功能，在效率与模块化之间权衡。')
c('原语的实现原理', '原语的原子性如何通过关中断保证？', '单CPU系统：关中断(STI/CLI)后执行不可被中断切换→执行完再开中断。因此原语连续不可分割。多CPU还需额外的锁机制(自旋锁)，因为关中断只影响当前CPU。')
c('守护进程', '什么是守护进程(Daemon)？与普通进程的区别？', '守护进程是后台运行的常驻进程，脱离终端控制(无控制tty)。特点：父进程为init(1)(或systemd)；无控制终端；长期在后台提供服务(httpd/sshd/crond)。创建：fork+setsid脱离会话成为新进程组和会话的首进程。')
c('孤儿进程与僵尸进程', '孤儿进程和僵尸进程的区别？如何避免僵尸进程？', '孤儿：父进程终止但子进程还在，由init收养善后。僵尸：子进程终止但父进程未调用wait()回收→子PCB保留(显示Z状态)，占用PID等。解决：父进程调用wait()/waitpid()；或忽略SIGCHLD信号；或fork两次使孙进程被init收养。')
c('实时操作系统', '硬实时和软实时操作系统的区别？', '硬实时：任务必须在期限内完成(超时系统失败，如航空控制/汽车刹车)。软实时：偶尔超时可容忍但影响品质(如视频播放/音频)。RTOS调度策略：RM(单调速率优先)和EDF(最早时限优先)。Linux PREEMPT_RT补丁支持软实时。')
c('临界区的软件实现', '关闭中断和Test-and-Set等硬件方案vs纯软件方案(Peterson)的区别？', '软件方案(Peterson)：纯算法无硬件支持，但仅限2进程，使用忙等。硬件TSL/Swap：原子指令支持，忙等(自旋)。关中断：仅单CPU，不可在用户态。信号量/管程是OS提供的无需忙等的复杂同步方案(阻塞/唤醒)。')
c('监控死锁的检测周期', '死锁检测的时机？每隔多长时间检测一次？', '检测时机：1.每次资源请求时检测(开销大) 2.定期检测(如每N分钟/当CPU利用率明显下降)。检测算法：化简资源分配图检查是否有环(对每个进程查看是否能完成)。开销大，一般只在有死锁症状时检测。')
c('连续内存分配的碎片合并', '动态分区如何合并空闲分区？边界标识法？', '回收时检查相邻分区：上邻空闲则合并(修改大小)。下邻空闲也合并(修改下一块的大小与起始地址)。边界标识法：在每个分区首尾记录分区状态(空闲/已占)，便于快速判断相邻分区状态和合并。合并可减少外部碎片数量。')
c('伙伴系统', '伙伴系统(Buddy System)的分配和回收原理？', '将内存按2的次幂大小划分成块。请求k分配2^ceil(log₂k)的块，不够则分割大的伙伴。释放时和空闲的伙伴合并(地址相邻且同大小即为伙伴)。分配/回收O(logn)，但有内部碎片(如请求9KB分配16KB)。')
c('虚拟内存的地址结构', '32位系统下典型虚拟地址结构(页号+页内偏移)？', '4KB页(12位偏移) + 20位虚页号 = 2^20=1M个虚页。4MB大页: 22位偏移 + 10位虚页号 = 1024个虚页。大页减少TLB项使用但内部碎片大。64位地址选48位虚拟地址(256TB), 四级页表(PGD/PUD/PMD/PTE)每级9位。')
c('页面大小选择', '页面大小应该取多大？太小和太大的影响？', '太小：页表大(页面数多)→TLB覆盖范围小→缺页多；空间碎片少。太大：内部碎片大(最后一页浪费多)但页表小。典型折衷值：4KB(4096B)。大数据/图形应用可支持大页(2MB/1GB)。')
c('请求分页的页面分配与调入策略', '预调页和请求调页策略的区别？', '请求调页(Demand Paging)：发生缺页时才调入(使用预调页只在缺页时调单页)。预调页(Prepaging)：缺页时一次调入相邻多页(利用空间局部性→一次缺页调入一组相关页)。预调可减少未来缺页但可能调入无用页。')
c('磁盘调度SSTF死锁', 'SSTF算法可能导致某些请求饥饿？', 'SSTF每次选离当前磁头最近位置→如果不断有新请求在磁头附近，那么远端的请求永远无法被服务。类似贪心算法在某些请求流中造成饥饿。SCAN/C-SCAN无饥饿但请求分布不均衡导致高的平均寻道时间。')
c('文件一致性检查的具体修复', 'fsck如何修复块分配的不一致？', '重复块(被多个文件指向)和丢失块(有块号标记为占用但未被任何文件引用→回收为空闲)。fsck扫描整个文件系统的inode→构建块使用表两次(块位表 vs 文件寻址块)。不一致则修复(复制重复块给所有文件;将丢失块回收到空闲列表)。')
c('文件系统的日志机制', '日志文件系统(Journaling)保证一致性的原理？', '写入更改前先在日志(磁盘专门的日志区域)记录即将执行的操作→然后提交日志(commit)→最后写入真正的文件系统(Checkpoint)。崩溃重启重放(replay)日志恢复尚未写入文件系统的操作。ext3/4的ordered模式只记元数据日志。')
c('设备无关性', '逻辑设备和物理设备的区别？LUT的作用？', '逻辑设备名(/dev/ttyUSB0) 对应用程序透明。OS通过设备表(LUT:逻辑设备表→驱动程序入口)将逻辑名映射到具体驱动。增加新设备只需新增驱动并加入LUT而无需改应用程序。')
c('I/O的数据传输单位', '字符设备和块设备的根本区别？', '字符设备(终端/键盘/鼠标)：以字符流方式传输，不支持随机寻址，一般无缓存或线性缓存。块设备(磁盘)：以块为单位传输(512B/4KB)，支持随机寻址，有块缓冲区。网络接口是第三种(结合两者，数据打包发送)。')
c('直接IO与缓冲IO', 'open()的O_DIRECT/O_SYNC的作用？', 'O_DIRECT：绕过页缓存直接与磁盘交互(数据对齐要求)，适用数据库自主管理缓存。O_SYNC：每次write同步写回(类似write()后fdatasync)，数据可靠但性能低。默认缓冲IO→写脏页异步写回。')

export default cards
